{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "A backend for the \"phonebook\" problem set",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf build && cd frontend/ && npm run build --prod && cp -r build ../"
  },
  "author": "Josh Cowan",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.12"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.13.5",
    "mongoose-unique-validator": "^2.0.3",
    "morgan": "^1.10.0"
  }
}
